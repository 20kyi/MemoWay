# 성능 진단 가이드

## 개요

이 프로젝트는 성능 모니터링 시스템이 내장되어 있어, 서버와 클라이언트의 성능 문제를 쉽게 진단할 수 있습니다.

## 기능

### 1. 서버 측 성능 모니터링

- **자동 로깅**: 모든 API 요청의 응답 시간이 자동으로 기록됩니다
- **느린 요청 감지**: 1초 이상 걸리는 요청은 경고로 표시됩니다
- **통계 수집**: 경로별 평균 응답 시간, 최대 응답 시간, 느린 요청 비율 등을 수집합니다

### 2. 성능 대시보드 (개발 모드)

개발 모드에서 `/api/performance/stats` 엔드포인트를 통해 성능 통계를 확인할 수 있습니다.

### 3. 콘솔 리포트

개발 모드에서는 10분마다 자동으로 성능 리포트가 콘솔에 출력됩니다.

## 사용 방법

### 서버 로그 확인

서버 콘솔에서 다음과 같은 형식으로 로그가 출력됩니다:

```
GET /api/memos 200 in 234ms
⚠️ [느린 요청 감지] POST /api/memos - 1234ms (상태: 200)
```

### 성능 대시보드 사용

개발 모드에서 브라우저 콘솔에서 다음을 실행:

```javascript
fetch('/api/performance/stats')
  .then(res => res.json())
  .then(console.log);
```

또는 성능 대시보드 컴포넌트를 사용하세요.

### 성능 로그 초기화

```javascript
fetch('/api/performance/clear', { method: 'POST' })
  .then(res => res.json())
  .then(console.log);
```

## 성능 최적화 팁

### 서버 측

1. **데이터베이스 쿼리 최적화**
   - 인덱스 확인
   - N+1 쿼리 문제 방지
   - 불필요한 JOIN 제거

2. **캐싱 전략**
   - 자주 조회되는 데이터 캐싱
   - React Query의 staleTime 설정 활용

3. **느린 API 엔드포인트 개선**
   - 대용량 데이터는 페이지네이션 적용
   - 필요한 필드만 조회

### 클라이언트 측

1. **컴포넌트 최적화**
   - `React.memo` 사용
   - `useMemo`로 무거운 계산 캐싱
   - `useCallback`으로 함수 재생성 방지

2. **렌더링 최적화**
   - 불필요한 리렌더링 방지
   - 가상화 (Virtualization) 적용

3. **네트워크 요청 최적화**
   - React Query 캐싱 활용
   - 불필요한 refetch 방지

## 문제 해결

### 느린 응답 시간이 감지되는 경우

1. **서버 로그 확인**
   - 어떤 엔드포인트가 느린지 확인
   - 평균 응답 시간과 최대 응답 시간 비교

2. **데이터베이스 쿼리 확인**
   - 쿼리 실행 계획 확인
   - 인덱스 사용 여부 확인

3. **네트워크 지연 확인**
   - 데이터베이스 연결 지연
   - 외부 API 호출 지연

### 클라이언트가 느린 경우

1. **React DevTools Profiler 사용**
   - 어떤 컴포넌트가 느린지 확인
   - 렌더링 시간 측정

2. **네트워크 탭 확인**
   - API 응답 시간 확인
   - 불필요한 요청 여부 확인

3. **메모리 사용량 확인**
   - 메모리 누수 확인
   - 큰 컴포넌트 최적화

## 일반적인 성능 문제

### 1. N+1 쿼리 문제

**증상**: 하나의 요청에 수백 개의 데이터베이스 쿼리가 실행됨

**해결**: JOIN을 사용하거나 `findMany`의 `with` 옵션 활용

### 2. 대용량 데이터 전송

**증상**: 단일 API 요청이 매우 큰 JSON을 반환

**해결**: 페이지네이션, 필드 선택, 압축 적용

### 3. 불필요한 리렌더링

**증상**: 작은 상태 변경만으로도 많은 컴포넌트가 리렌더링됨

**해결**: `React.memo`, `useMemo`, `useCallback` 활용

## 성능 기준

- **빠른 응답**: < 200ms
- **정상 응답**: 200ms - 500ms
- **느린 응답**: 500ms - 1000ms
- **매우 느린 응답**: > 1000ms (개선 필요)

## 추가 리소스

- [React 성능 최적화](https://react.dev/learn/render-and-commit)
- [Node.js 성능 모니터링](https://nodejs.org/en/docs/guides/simple-profiling/)
- [Drizzle ORM 최적화](https://orm.drizzle.team/docs/performance)
