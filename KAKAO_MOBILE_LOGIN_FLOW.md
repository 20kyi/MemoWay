# 모바일 앱 카카오 로그인 플로우

## 목표 플로우

```
1. 사용자(앱) → 카카오 인증 페이지
2. 카카오 → 웹 리다이렉트(인가 코드 포함)
3. 웹(프런트) → 서버(Express)로 인가코드 전달
4. 서버가 카카오 토큰·프로필 요청
5. DB(Postgres)에 사용자 생성/갱신
6. 서버가 자체 세션/JWT 발급
7. 웹에서 딥링크로 네이티브 앱 실행(토큰 전달)
8. 네이티브(앱)에서 세션 적용
```

## 현재 구현 상태

### ✅ 완료된 부분
- 카카오 OAuth 인증 시작 (`/api/kakao/login`)
- 카카오 콜백 처리 (`/api/kakao/callback`)
- 세션 생성 및 사용자 저장
- 딥링크 리다이렉트 (`/api/kakao/redirect`)
- 앱에서 딥링크 처리 및 세션 동기화

### 🔄 개선이 필요한 부분
현재는 서버에서 직접 콜백을 처리하고 있지만, 목표 플로우는:
- 프런트엔드에서 인가 코드를 받아서
- 서버로 전달하는 방식

## 구현 계획

### 1. 새로운 엔드포인트 추가
`/api/kakao/exchange-code` - 인가 코드를 받아서 토큰 교환 및 세션 생성

### 2. 프런트엔드 콜백 페이지
`/kakao-callback` - 카카오에서 리다이렉트된 인가 코드를 받아서 서버로 전달

### 3. 딥링크에 토큰 포함
세션 쿠키 대신 또는 추가로 JWT 토큰을 딥링크에 포함하여 전달

## 상세 구현

### 서버 엔드포인트: `/api/kakao/exchange-code`

**요청:**
```json
{
  "code": "인가코드",
  "state": "CSRF 토큰",
  "lang": "ko"
}
```

**응답:**
```json
{
  "success": true,
  "user": {
    "id": "kakao_123456",
    "email": "user@example.com",
    ...
  },
  "sessionId": "세션ID",
  "token": "JWT토큰 (선택사항)"
}
```

### 프런트엔드 콜백 페이지

1. URL에서 `code`와 `state` 파라미터 추출
2. 서버의 `/api/kakao/exchange-code`로 POST 요청
3. 성공 시 딥링크로 앱으로 리다이렉트
4. 실패 시 에러 메시지 표시

### 딥링크 형식

**성공:**
```
com.memoway.app://login?success=true&sessionId=xxx&token=yyy
```

**실패:**
```
com.memoway.app://login?error=xxx&message=yyy
```

## 보안 고려사항

1. **CSRF 보호**: `state` 파라미터 검증
2. **인가 코드 일회성**: 카카오에서 제공하는 인가 코드는 한 번만 사용 가능
3. **토큰 만료**: JWT 토큰 사용 시 적절한 만료 시간 설정
4. **HTTPS 필수**: 모든 통신은 HTTPS로

## 테스트 시나리오

1. 앱에서 카카오 로그인 버튼 클릭
2. 카카오 인증 페이지로 이동
3. 로그인 완료 후 웹으로 리다이렉트 (인가 코드 포함)
4. 프런트엔드에서 인가 코드 추출
5. 서버로 인가 코드 전달
6. 서버에서 토큰/프로필 요청 및 세션 생성
7. 딥링크로 앱으로 리다이렉트
8. 앱에서 세션 적용 확인
